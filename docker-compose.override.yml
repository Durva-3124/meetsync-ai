version: "3.9"
services:
  ai-service:
    volumes:
      - ./ai-service/app:/app/app:ro
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    environment:
      - PYTHONUNBUFFERED=1
